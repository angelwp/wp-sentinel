ANTHROPIC_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
# Secreto aleatorio de 32 bytes (64 caracteres hex). Generarlo en una terminal
# propia, nunca en un chat: python3 -c "import secrets; print(secrets.token_hex(32))"
IP_HASH_SALT=
DAILY_BUDGET_USD=
ENVIRONMENT=
